# Optional manual overrides.
# Transaction IDs are generated by the CLI into transactions.json.
# Example:
# overrides:
#   tx_1234abcd: Entertainment
# group_overrides:
#   tx_1234abcd: Subscriptions
#
# Narrative contains override (case-insensitive substring match):
# narrative_contains:
#   spotify: Subscriptions
# group_narrative_contains:
#   spotify: Subscriptions
overrides: {}
narrative_contains: {}
group_overrides: {}
group_narrative_contains: {}
